//interfaces is like a type but exculevely for object type

// type Person = {
//     name: string;
//     age: number;
// }

interface Person {
  name: string;
  age: number;
}

const pr: Person = { name: "hemd", age: 34 };
